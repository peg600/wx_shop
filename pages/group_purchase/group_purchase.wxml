<!--pages/group_purchase/group_purchase.wxml-->
<!-- <view class='top' wx:if="{{group_purchase_id}}">
  <view class='goods-info-wrapper' bindtap='gotoDetail'>
    <view class='left'>
      <view class='pic-wrapper'>
        <image src='{{goods_info.goods_img}}'></image>
      </view> 
    </view>
    <view class='right'>
      <view class='price'>
        <view class='bargain'>
          限时立省{{(goods_info.shop_price - goods_info.act_price)/goods_info.shop_price*100|int}}%
        </view>
        <view class='bargain'>
          限时立省￥{{goods_info.goods_discount - goods_info.shop_price}}
        </view>
        <view class='shop-price'>￥{{goods_info.goods_discount}}</view>
        <view class='act-price'>￥{{goods_info.shop_price}}</view>
      </view>
      <view class='users'>
        <view class='captain-wrapper'>
          <view class='captain'>
            <image src='{{users_info[0].avatar_url}}'></image>
          </view>
          <view class='captain-mark'>拼主</view>
        </view>  
        <view class='others'>
          ?
        </view>
      </view>
      <view class='buy'>
        <text>点击购买</text>
      </view>
    </view>
  </view>
</view> -->

<view class='coupon'>
  <view class='coupon-left'>
    <view>￥{{info.goods_discount}}</view>
  </view>
  <view class='coupon-right'>仅售{{info.shop_price}}元</view>
</view>

<view class='invite' wx:if="{{inviter_info}}">
    <view class='inviter'>
      <image src='{{inviter_info.avatar_url}}'></image>
      <view>{{inviter_info.nick_name}}邀请你拼单</view>
    </view>
    <button class='share-btn' type='warn' wx:if="{{inviter_info.timer != '' && inviter_info.status == 1}}" data-group_purchase_id="{{inviter_info.group_purchase_id}}" catchtap='joinCashCouponGroupPurchase'>我要拼单, {{inviter_info.timer}} 秒后结束</button>
    <view class='share-btn gray' type='warn' wx:else>该团已结束</view>
    <!-- <text>分享到3个群后，拼成率高达98%</text> -->
  </view>

<view class='title'>现金券购买须知：</view>
<view class='rules'>
  <view>1.现金券需两人拼团购买，在消费时可用于抵扣现金</view>
  <view>2.拼团时限为24小时，超时未成团则此团废弃，已支付金额将退还给用户</view>
  <view>3.现金券不可与优惠券同时使用</view>
  <view>4.若消费中抵扣的金额小于现金券的面值，则剩余部分转存在用户现金券余额中，之后的消费中仍可使用</view>
  <view>5.用户现金券余额不可提现</view>
</view>

<view class='middle'>
  <view class='failed' wx:if="{{!inviter_info || inviter_info.status != 1 || inviter_info.timer == ''}}">
    <view class='join' catchtap='payNewCashCouponGroupPurchase'>
      <view>我来发起拼单</view>
    </view>

    <view class='group-list' wx:if='{{group_list.length > 0}}'>
      <text>
        ———— 或参加别人的拼单 ————
      </text>
      <view class='group-item-list'>
        <view class='group-item' wx:for="{{group_list}}" wx:key="{{group.group_purchase_id}}" wx:for-item="group" data-order_id="{{group.group_purchase_id}}" wx:if='{{group.timer !== ""}}'>
          <view class='group-left'>
            <image src='{{group.avatar_url}}'></image>
            <text>{{group.nick_name}}</text>
          </view>
          <view class='group-right'>
            <view class='group-info'>
              <view class='need-number'>还差1人拼成</view>
              <view class='group-time-count'>剩余 {{group.timer}}</view>
            </view>
            <view class='group-join' data-group_purchase_id="{{group.group_purchase_id}}" catchtap='joinCashCouponGroupPurchase'>
              去拼单
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <!-- <view class='info-list'>
    <view class='info-left'>
    商品名称
    </view>
    <view class='info-right'>
      <view class='jump-wrapper' bindtap='gotoDetail'>
        <view class='goods-name'>
          {{goods_info.goods_name}}
        </view>
        <view class='jump-icon'>
          <image src='/pages/img/jump_icon.png'></image>
        </view>
      </view>
    </view>
  </view>
  <view class='info-list'>
    <view class='info-left'>
      拼单时间
    </view>
    <view class='info-right'>
      {{current_order_info.act_start_time}}
    </view>
  </view>
  <view class='info-list'>
    <view class='info-left'>
      拼单须知
    </view>
    <view class='info-right'>
      <ul class='rule-info'>
        <li class='rules'>
          好友拼单
        </li>
        <li class='rules'>
          人满发货
        </li>
        <li class='rules'>
          人不满退款
        </li>
      </ul>
    </view>
  </view> -->
</view>



<view class='mask' wx:if="{{show_share}}" catchtap='hide'>
  <view class='share'>
    <view>开团成功！点击邀请好友分享</view>
    <button open-type='share'>邀请好友</button>
  </view>
</view>
